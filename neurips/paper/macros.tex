% Shared macros for PEM paper.
\newcommand{\pem}{\textsc{pem}}
\newcommand{\rhof}{\rho_f}
\newcommand{\Loss}{\mathcal{L}}
\newcommand{\LPEM}{\Loss_{\mathrm{PEM}}}
\newcommand{\unlabeled}{\mathcal{U}}
\newcommand{\labeled}{\mathcal{L}}

% Placeholder references to avoid undefined refs during early drafts.
% Remove these once the Experiments section writes the actual table/figure.
\providecommand{\reftabentropy}{Tab.~\ref{tab:entropy}}
\providecommand{\reftabmain}{Tab.~\ref{tab:main}}
