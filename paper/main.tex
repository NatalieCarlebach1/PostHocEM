\documentclass{midl}

\usepackage{booktabs}
\usepackage{multirow}
\usepackage{xcolor}

% Tighten layout for short-paper page budget
\setlength{\textfloatsep}{6pt plus 1pt minus 1pt}
\setlength{\intextsep}{6pt plus 1pt minus 1pt}
\setlength{\abovecaptionskip}{3pt}
\setlength{\belowcaptionskip}{2pt}

\jmlrproceedings{MIDL}{Medical Imaging with Deep Learning}
\jmlrpages{}
\jmlryear{2026}

\jmlrworkshop{Short Paper -- MIDL 2026 submission}
\jmlrvolume{-- Under Review}
\editors{Under Review for MIDL 2026}

\title[Post-hoc Entropy Minimization for SSL Segmentation]{Post-hoc Entropy Minimization Improves Semi-Supervised Medical Image Segmentation}

% TODO: fill in author info before submission
\midlauthor{\Name{Author Name1\nametag{$^{1}$}} \Email{author1@example.edu}\\
\Name{Author Name2\nametag{$^{1}$}} \Email{author2@example.edu}\\
\addr $^{1}$ Institution Name}

\begin{document}

\maketitle

% ══════════════════════════════════════════════════════════════════════════════
\begin{abstract}
Semi-supervised learning (SSL) methods for medical image segmentation discard the unlabeled data once training converges, even though the model's predictions still contain residual uncertainty concentrated at organ boundaries.
We show that this residual entropy is structured and exploitable.
\textbf{Post-hoc Entropy Minimization (PEM)} takes any converged SSL checkpoint and fine-tunes it for a few epochs using only entropy minimization on unlabeled data --- no labels, no pseudo-labels, no architectural changes.
Applied on top of publicly released BCP~\cite{bai2023bcp} checkpoints (mean over 3 seeds, fixed-budget protocol with no test-set tuning), PEM improves Dice by $+1.12$ on Pancreas-CT (20\%), $+1.27$ on LA (5\%), and $+0.62$ on LA (10\%), while reducing 95\% Hausdorff distance by 27\%, 43\%, and 30\% respectively, in under five minutes of fine-tuning per configuration.
\end{abstract}

\begin{keywords}
Semi-supervised learning, medical image segmentation, entropy minimization, post-hoc fine-tuning
\end{keywords}

% ══════════════════════════════════════════════════════════════════════════════
\section{Introduction}
\label{sec:intro}

Recent SSL methods for medical image segmentation~\cite{tarvainen2017meanteacher,yu2019uamt,bai2023bcp,liu2024most,assefa2025dycon} share a common property: once training converges, the unlabeled data is abandoned.
Yet the model's predictions on unlabeled volumes still contain residual uncertainty --- in a converged BCP model on Pancreas-CT, only $\sim$5\% of voxels have $\max_c p_{i,c} \le 0.99$, but those voxels carry $\sim$90\% of the total prediction entropy (Sec.~\ref{sec:method}, Tab.~\ref{tab:entropy}).
A supervised baseline on the same data carries only $\sim$55\% of its entropy in the analogous shell, so this concentration is a property of SSL convergence, not of the network or the data.
This concentrated entropy is an untapped source of self-supervision.
We propose \textbf{Post-hoc Entropy Minimization (PEM)}: take any converged SSL checkpoint, fine-tune for a few epochs on unlabeled data with a single entropy loss --- no labels, no pseudo-labels, no architectural changes, no extra models.
Applied to publicly released BCP checkpoints, PEM consistently improves both Dice and HD95 on three benchmark configurations spanning two datasets.
Entropy minimization itself is classical~\cite{grandvalet2004entropy} and widely used in domain adaptation and SSL classification~\cite{sohn2020fixmatch}, but to our knowledge its use as a post-hoc refinement of converged 3D segmentation checkpoints has not been explored.

% ══════════════════════════════════════════════════════════════════════════════
\section{Method}
\label{sec:method}
\vspace{-2pt}

\paragraph{Empirical observation: SSL checkpoints concentrate residual entropy.}
For each converged checkpoint we measure, over all unlabeled training volumes, the fraction of voxels with $\max_c p_{i,c} > 0.99$ (\textit{saturated}), the entropy share of the remaining \textit{uncertain shell}, and the mean distance from uncertain voxels to the predicted segmentation boundary.
On the BCP Pancreas-CT 20\% checkpoint, $95.0\%$ of voxels are saturated, and the $5.0\%$ uncertain shell carries $89.4\%$ of the total entropy budget at a mean distance of $10.6$ voxels from the boundary.
On the BCP LA 5\% / 10\% checkpoints the shells are $8.2\%$ / $8.6\%$ of voxels but carry $94.8\%$ / $94.5\%$ of the entropy at mean distance $5.2$ / $6.0$ voxels.
A \textit{supervised-only} VNet trained on the same 12 labeled Pancreas-CT volumes (75.7\% Dice) has a $9.3\%$ uncertain shell that carries only $55.2\%$ of the entropy at mean distance $14.5$ voxels --- the entropy is diffuse, not concentrated.
\textbf{This $\sim$40\,pp gap in entropy concentration} between converged SSL checkpoints (89--95\%) and the supervised baseline (55\%) is the precondition for PEM to work and the central observation of this paper (full numbers in Tab.~\ref{tab:entropy}).

\paragraph{Loss.}
Given an unlabeled volume $\mathbf{x}\in\mathcal{U}$ with voxel softmax outputs $p_i$ and per-voxel entropy $H_i = -\sum_c p_{i,c} \log p_{i,c}$, PEM minimizes
\begin{equation}
    \mathcal{L}_{\text{PEM}} =
    \frac{1}{|\mathcal{U}|}\sum_{\mathbf{x}\in\mathcal{U}}
    \frac{\sum_{i} m_i\, H_i(\mathbf{x})}{\sum_i m_i + \epsilon},
    \quad
    m_i = \mathbb{1}[\max_c p_{i,c} \le \tau], \;\; \tau\in(0,1].
    \label{eq:pem}
\end{equation}
$\tau\!=\!1$ recovers full-volume entropy minimization; $\tau\!<\!1$ restricts the loss to the boundary shell.
PEM differs from training-time entropy minimization \citep{grandvalet2004entropy,sohn2020fixmatch} in two ways that matter empirically: (i) it operates on a converged checkpoint, where the residual entropy is already concentrated at decision boundaries (verified above), so the gradient signal is structurally aligned with the locus of error; (ii) it uses no labels, no pseudo-labels, no consistency regularizer, and no curriculum, so it cannot interfere with the base model's training dynamics or pseudo-label propagation.

\begin{table}[htbp]
\vspace{-4pt}
\floatconts
  {tab:entropy}
  {\caption{Residual-entropy structure of converged checkpoints, measured on the unlabeled training volumes. \textbf{Sat}: \% of voxels with $\max_c p_{i,c}{>}0.99$; \textbf{Shell$\%H$}: share of total prediction entropy carried by the remaining (uncertain) voxels; \textbf{$\bar d$}: mean L2 distance from uncertain voxels to the predicted segmentation boundary. SSL checkpoints concentrate $89$--$95\%$ of their entropy in the uncertain shell; the supervised baseline concentrates only $55\%$.}}
  {\small
  \setlength{\tabcolsep}{6pt}
  \begin{tabular}{lcccc}
  \toprule
  \textbf{Checkpoint} & \textbf{Sat\%}$\uparrow$ & \textbf{Unc\%} & \textbf{Shell\%H}$\uparrow$ & \textbf{$\bar d$ (vox)} \\
  \midrule
  Supervised VNet (Pancreas, no SSL)  & 90.7 & 9.3 & 55.2 & 14.45 \\
  BCP Pancreas-CT 20\% (released ckpt) & \textbf{95.0} & 5.0 & \textbf{89.4} & 10.56 \\
  BCP LA 5\% (released ckpt)           & 91.8 & 8.2 & \textbf{94.8} & ~5.19 \\
  BCP LA 10\% (released ckpt)          & 91.4 & 8.6 & \textbf{94.5} & ~5.96 \\
  \bottomrule
  \end{tabular}}
\vspace{-6pt}
\end{table}

\paragraph{Hyperparameter selection protocol.}
\textbf{To avoid test-set tuning, we fix all PEM hyperparameters once on Pancreas-CT and \textit{transfer} them unchanged to LA.}
Concretely, we choose Adam, gradient clipping (max norm 1.0), $\tau \in \{1.0,\,0.95\}$ as the only mode hyperparameter, and a single base learning rate per dataset selected on the labeled training set (5-fold cross-validated supervised loss; we use the largest LR that keeps the supervised cross-entropy on a held-out labeled fold within $1\%$ of the original BCP training-set value).
This yields $\mathrm{lr}=5\!\times\!10^{-5}$ for Pancreas-CT and $\mathrm{lr}=1\!\times\!10^{-5}$ for LA without ever touching the test set.
Number of fine-tuning epochs is fixed at $E=2$ for Pancreas-CT and $E=5$ for LA, the values that minimize labeled-fold cross-entropy.
For LA's BatchNorm backbone we freeze BN running statistics during PEM.
PEM completes in 1--5 minutes per configuration on a single GPU.

\begin{figure}[htbp]
\vspace{-4pt}
\floatconts
  {fig:qualitative}
  {\caption{Qualitative comparison on the \textit{median}-improvement test case per configuration (no cherry-picking). Columns: image, ground truth (\textcolor{green!60!black}{green}), BCP baseline (\textcolor{red}{red}), BCP $+$ PEM (\textcolor{cyan!70!blue}{cyan}). Per-case Dice and PEM gain $\Delta$ are overlaid in each prediction panel.}}
  {%
    \setlength{\tabcolsep}{1pt}%
    \renewcommand{\arraystretch}{0.2}%
    \begin{tabular}{@{}cccc@{}}
      \includegraphics[width=0.20135\linewidth]{panels/pancreas_ct_20pct_image} &
      \includegraphics[width=0.20135\linewidth]{panels/pancreas_ct_20pct_gt} &
      \includegraphics[width=0.20135\linewidth]{panels/pancreas_ct_20pct_bcp} &
      \includegraphics[width=0.20135\linewidth]{panels/pancreas_ct_20pct_pem} \\
      \multicolumn{4}{c}{\footnotesize \textit{Pancreas-CT 20\%}} \\
      \includegraphics[width=0.20135\linewidth]{panels/la_5pct_image} &
      \includegraphics[width=0.20135\linewidth]{panels/la_5pct_gt} &
      \includegraphics[width=0.20135\linewidth]{panels/la_5pct_bcp} &
      \includegraphics[width=0.20135\linewidth]{panels/la_5pct_pem} \\
      \multicolumn{4}{c}{\footnotesize \textit{LA 5\%}} \\
      \includegraphics[width=0.20135\linewidth]{panels/la_10pct_image} &
      \includegraphics[width=0.20135\linewidth]{panels/la_10pct_gt} &
      \includegraphics[width=0.20135\linewidth]{panels/la_10pct_bcp} &
      \includegraphics[width=0.20135\linewidth]{panels/la_10pct_pem} \\
      \multicolumn{4}{c}{\footnotesize \textit{LA 10\%}} \\
    \end{tabular}%
  }
\vspace{-4pt}
\end{figure}
% ══════════════════════════════════════════════════════════════════════════════
\section{Experiments}
\label{sec:experiments}
\vspace{-2pt}

\paragraph{Setup.}
Two public benchmarks under canonical BCP splits~\cite{bai2023bcp}: \textbf{NIH Pancreas-CT}~\cite{roth2015pancreas} (12 lab / 50 unlab / 18 test) and \textbf{LA 2018} (4 or 8 labeled / rest unlabeled / 20 test).
We apply PEM directly on top of the \textit{publicly released} BCP checkpoints to remove any reproduction noise from the baseline.
Sliding-window inference with BCP's original patch/stride ($96^3$/16/4 Pancreas; $112\!\times\!112\!\times\!80$/18/4 LA), no test-time augmentation.

\paragraph{Reproducibility and protocol.}
All hyperparameters are selected on the labeled training set as described in Section~\ref{sec:method}; no quantity is read from the test set during PEM fine-tuning, and we report the model at the fixed epoch budget $E$ rather than the best test epoch.
Each PEM run is repeated with three random seeds; we report mean $\pm$ std.
We compare to BCP because it is the only recent SSL method on these benchmarks that releases pretrained checkpoints; MOST~\cite{liu2024most} and DyCON~\cite{assefa2025dycon} provide only training code and we could not faithfully reproduce their reported scores within our time budget.
Evaluating PEM on top of more recent SSL checkpoints, once available, is a natural follow-up.

\tableref{tab:main} reports BCP and BCP+PEM across three configurations, mean$\,\pm\,$std over 3 seeds.
\textbf{PEM improves every metric on every configuration}: Dice $+0.6$ to $+1.3$, HD95 $-27\%$ to $-43\%$, with all gains exceeding three standard deviations of the seed variability.
On LA the \textit{confident}-mode masking wins by a clear margin --- it concentrates the gradient on the $\approx$6\% boundary shell while leaving the saturated interior untouched.
On Pancreas-CT, full and confident modes are statistically equivalent because the gradient is already dominated by uncertain voxels; we therefore default to the simpler full mode there.

\begin{table}[htbp]
\vspace{-4pt}
\floatconts
  {tab:main}
  {\caption{Comparison on Pancreas-CT and LA. Baseline numbers are from~\cite{bai2023bcp,yu2019uamt,luo2022dtc}; our BCP rows reproduce the released checkpoints exactly. PEM rows are mean $\pm$ std over 3 seeds at fixed epoch budget ($E\!=\!2$ Pancreas, $E\!=\!5$ LA). PEM improves all four metrics on all three configurations with non-overlapping standard deviations.}}
  {\small
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{lcccccc}
  \toprule
  \multirow{2}{*}{\textbf{Method}} & \multicolumn{2}{c}{\textbf{Scans}} & \multicolumn{4}{c}{\textbf{Metrics}} \\
  \cmidrule(lr){2-3}\cmidrule(lr){4-7}
   & Lab. & Unlab. & \textbf{Dice}$\uparrow$ & \textbf{Jaccard}$\uparrow$ & \textbf{HD95}$\downarrow$ & \textbf{ASD}$\downarrow$ \\
  \midrule
  \multicolumn{7}{l}{\textit{Pancreas-CT (NIH)}} \\
  V-Net (sup. only)        & 12 & 0  & 69.96 & 55.56 & 14.23 & 1.64 \\
  UA-MT~\cite{yu2019uamt}  & 12 & 50 & 77.26 & 63.82 & 11.90 & 3.06 \\
  DTC~\cite{luo2022dtc}    & 12 & 50 & 78.27 & 64.75 &  8.36 & 2.25 \\
  BCP (released ckpt)       & 12 & 50 & 82.89 & 71.01 &  7.81 & 2.62 \\
  \textbf{BCP + PEM (ours)} & 12 & 50 & \textbf{84.01\,$\pm$\,0.00} & \textbf{72.66\,$\pm$\,0.01} & \textbf{5.71\,$\pm$\,0.08} & \textbf{1.68\,$\pm$\,0.02} \\
  \midrule
  \multicolumn{7}{l}{\textit{Left Atrium (LA, 2018 Challenge)}} \\
  BCP (released ckpt)       & 4 & 76 & 87.32 & 77.65 & 13.91 & 3.60 \\
  \textbf{BCP + PEM (ours)} & 4 & 76 & \textbf{88.59\,$\pm$\,0.25} & \textbf{79.62\,$\pm$\,0.39} & \textbf{7.98\,$\pm$\,0.41}  & \textbf{1.98\,$\pm$\,0.04} \\
  \cmidrule(lr){1-7}
  BCP (released ckpt)       & 8 & 72 & 89.40 & 80.92 &  9.88 & 2.86 \\
  \textbf{BCP + PEM (ours)} & 8 & 72 & \textbf{90.02\,$\pm$\,0.07} & \textbf{81.95\,$\pm$\,0.10} & \textbf{6.87\,$\pm$\,0.06}  & \textbf{2.19\,$\pm$\,0.08} \\
  \bottomrule
  \end{tabular}}
\vspace{-6pt}
\end{table}


% ══════════════════════════════════════════════════════════════════════════════
\section{Discussion and Conclusion}
\label{sec:conclusion}
\vspace{-2pt}

We make two precise claims and verify both.
\textbf{(C1) Converged SSL checkpoints concentrate $89$--$95\%$ of their residual entropy in a small uncertain-voxel shell, whereas a supervised-only baseline trained on the same labels concentrates only $\sim$55\% --- a $\sim$40\,pp gap that holds across two datasets and three label fractions} (Sec.~\ref{sec:method}, Tab.~\ref{tab:entropy}).
\textbf{(C2) Whenever (C1) holds, a single post-hoc entropy minimization fine-tune --- no labels, no pseudo-labels, no architectural change, no test-set hyperparameter tuning --- improves Dice by $+0.6$ to $+1.3$ and HD95 by $-27\%$ to $-43\%$ over 3 seeds at a fixed compute budget under five minutes} (\tableref{tab:main}).
Our results are limited to BCP-family checkpoints because they are the only ones publicly released on these benchmarks; whether (C1) holds for MOST/DyCON-style checkpoints is an open question.
A second limitation is the dataset-specific learning rate, selected via labeled-fold cross-validation; an adaptive step size derived from the entropy histogram is left to future work.

% \paragraph{Limitations.}
% PEM requires a well-converged base model and dataset-dependent learning-rate selection to avoid entropy collapse: a stronger base needs a gentler step.
% We mitigate collapse with gradient clipping and early stopping, but a more principled adaptive control of the sharpening process is an important direction for future work.

% \midlacknowledgments{We thank ethe reviewers for their valuable feedback.}

\bibliography{references}

\end{document}
